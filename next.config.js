// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  // 여기에 Next.js 설정을 추가합니다.
  // 예시:
  // reactStrictMode: true,
  // swcMinify: true,
  // images: {
  //   domains: ['example.com'],
  // },
};

module.exports = nextConfig;
